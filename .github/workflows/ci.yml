name: CI Test

on: [push]

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Hello
        run: echo "Si ce texte est présent, c'est que ça fonctionne!"
